package com.example.dietapp.ui.dish.screen

enum class DishScreenList(val title: String) {
    DishList(title = " Dish list"),
    Dish(title = " Add dish"),
    AddIngredientToDish(title = " Add ingredient")
}